{
  "name": "nodecache",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "precommit": [
    "npm run test:cache"
  ],
  "scripts": {
    "lint": "eslint ./src/**/*.js ./__tests__/**/*.js",
    "test": "vitest run",
    "test:cache": "vitest run & npm run lint",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@vitest/coverage-istanbul": "^1.1.3",
    "eslint": "^8.56.0",
    "vitest": "^1.1.3"
  }
}
